# autopilot-canary-20260723
Disposable Autopilot OSS maintainer canary

## Deterministic greeting

`GREETING.md` is the canonical deterministic greeting. Run
`./check-greeting.sh` to verify its bytes exactly.
